@model IEnumerable<Org.Ktu.Isk.P175B602.Autonuoma.Models.Modelis.ModelisL>

@* this will also be used as page title in main layout *@
@{ ViewData["title"] = "Modeliai"; }

@* content header *@
<div class="main-content-header">
	<span class="title">@ViewData["title"]</span>
	<span class="actions">
		<a href="@Url.Action("Create")" class="btn btn-primary"><i class="fa-solid fa-plus"></i> Sukurti naują</a>
	</span>
</div>

@* content itself *@
<div class="main-content-lst">
	<table class="entity-list">
		<thead>
			<tr>
				<td>
					@Html.DisplayNameFor(m => m.Pavadinimas)
				</td>
				<th>
					@Html.DisplayNameFor(m => m.IsleidimoMetai)
				</th>
				<th></th>
			</tr>
		</thead>
		<tbody>
			@foreach (var item in Model) {
			<tr>
				<td>
					@Html.DisplayFor(m => item.Pavadinimas)
				</td>
				<th>
					@Html.DisplayFor(m => item.IsleidimoMetai)
				</th>
				<td>
					<a 
						href="@Url.Action("Edit", new { id = item.Pavadinimas })" 
						class="btn btn-primary"
						><i class="fa-solid fa-pen-to-square"></i> Redaguoti</a>
						
					<a 
						href="@Url.Action("Delete", new { id = item.Pavadinimas })" 
						class="btn btn-danger"
						><i class="fa-solid fa-trash"></i> Šalinti</a>					
				</td>
			</tr>
			}
		</tbody>
	</table>
</div>